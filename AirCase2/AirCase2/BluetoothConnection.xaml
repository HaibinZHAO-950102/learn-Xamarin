<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:AirCase2.Models"
             x:Class="AirCase2.BluetoothConnection">

    <!--<ContentPage.BindingContext>
        <vm:DeviceViewModel></vm:DeviceViewModel>
    </ContentPage.BindingContext>-->

    <ContentPage.Content>
        <StackLayout Padding="20">
            <!--<ListView x:Name="btlist"
                  ItemSelected="btlist_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding NativeDevice.Name}"
                                  Detail="{Binding NativeDevice.Id}"
                                  TextColor="#404040">
                        </TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>-->

            <!--<Button Text="Scan"
                    BackgroundColor="#BFBFBF"
                    Clicked="Button_Clicked"></Button>-->
        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>
